# MVP Status

最終更新: 2026-06-28

このドキュメントは Stack Library の MVP 進捗を記録するための作業メモです。

README はプロジェクト思想と技術方針の安定情報として扱い、進捗や次の実装優先度はこちらで管理します。

## 現在の到達度

MVP 全体の目安は **60〜65%** です。

モックデータで静的サイトとして閲覧できる範囲では **70%前後** まで進んでいます。

microCMS 接続、知識マップ、検索・表示切り替えの完成まで含めると、まだ MVP 完了前の中盤です。

## 確認済みの状態

- `npm run build` が成功する
- `astro check` は 0 errors / 0 warnings
- 静的ビルドで 28 ページが生成される
- 主要な MVP ルートは一通り存在する
- モックデータで主要ページを表示できる
- `/books/[slug]/` は Figma の磨き込み版に合わせた詳細レイアウトになっている
- 書籍詳細の読書シグナル行は `BookSignalStats.astro` としてコンポーネント化済み

## 完了していること

- Astro プロジェクト構成
- TypeScript の主要型定義
- microCMS 想定の共通型
- モックデータ
- `src/lib/queries/` の初期データ取得関数
- デザイントークンとグローバル CSS の初期実装
- 共通レイアウトとナビゲーション
- `/` 概要ページ
- `/library/` 技術書ライブラリ
- `/books/[slug]/` 技術書詳細
  - Figma `03_書籍詳細ページ` の磨き込み版に準拠
  - `240px / 380px / 1fr` のデスクトップ構成
  - `BookSignalStats.astro` による横並び読書シグナル
  - `// why i read this`, `// insight 01`, `// related books` のコードコメント風本文ブロック
- `/topics/` 技術トピック一覧
- `/topics/[slug]/` 技術トピック詳細
- `/notes/` 読書メモ一覧
- `/notes/[slug]/` 読書メモ詳細
- `/collections/` 学習ルート一覧
- `/collections/[slug]/` 学習ルート詳細
- `/library/` のフィルター初期版
- `/library/` のグリッド / リスト表示切り替え初期版
- 書籍詳細から関連メモ・関連書籍へ戻る導線
- トピック詳細から関連書籍・関連メモへ戻る導線
- `docs/DESIGN.md`, `docs/COMPONENTS.md`, `docs/ROUTING.md` に書籍詳細ページの現行仕様を反映

## 未完了・弱いところ

- microCMS 本接続
- microCMS 未接続時と接続時の切り替え方針
- 知識マップ `/map/` の初期版
- ライブラリ検索
- URL クエリと検索・フィルター状態の同期
- 技術トピックによるライブラリ絞り込み
- Table / Board / Matrix 表示
- `NoteCard` コンポーネント
- `CollectionCard` コンポーネント
- `LibraryExplorer` React Island への切り出し
- `KnowledgeMap` / `KnowledgeNode` / `DetailSidePanel`
- メモ・学習ルート画面の情報密度と見た目の底上げ
- トピック詳細・メモ詳細・学習ルート詳細のFigma準拠レベルの磨き込み
- モックデータの拡充
- 空状態とエラー状態の全ページ整備
- アクセシビリティの全体確認

## 次にやるとよいこと

1. 知識マップ初期版を実装する
2. `/library/` の検索と URL クエリ同期を完成させる
3. 技術トピックによる絞り込みを追加する
4. トピック詳細・メモ詳細・学習ルート詳細の見た目を、書籍詳細と同じ密度まで底上げする
5. `NoteCard` と `CollectionCard` を分離する
6. モックデータを MVP 要件に近い量まで増やす
7. microCMS 取得関数を本実装へ切り替える
8. 全ページの空状態・フォーカス状態・見出し階層を確認する

## 完了判断に近づく条件

- microCMS 接続または明確なモックフォールバックがある
- 主要ページがすべてリンクで辿れる
- ライブラリ検索・フィルターが実用できる
- 知識マップで本、トピック、メモ、学習ルートの関係が見える
- 日本語 UI の読みやすさが全ページで破綻していない
- 状態やシグナルが色だけで表現されていない
- `npm run build` が継続して成功する
