import { Meta } from "@storybook/addon-docs/blocks";

export const lightColors = [
  ["背景", "--color-canvas", "#f7f9fc"],
  ["面", "--color-surface", "#ffffff"],
  ["本文", "--color-text", "#17202a"],
  ["補助本文", "--color-text-muted", "#64707d"],
  ["罫線", "--color-rule", "#d7dee7"],
  ["強調", "--color-accent", "#2457d6"],
  ["強調面", "--color-accent-soft", "#e8eeff"],
  ["蔵書強調", "--color-play", "#8d6029"],
];

export const darkColors = [
  ["背景", "--color-canvas", "#0b1115"],
  ["面", "--color-surface", "#111a20"],
  ["本文", "--color-text", "#eaf0f3"],
  ["補助本文", "--color-text-muted", "#7f8d99"],
  ["罫線", "--color-rule", "#2c3940"],
  ["強調", "--color-accent", "#54a8ff"],
  ["強調面", "--color-accent-soft", "#102c45"],
  ["蔵書強調", "--color-play", "#c08a4c"],
];

export const ColorBlocks = ({ colors }) => (
  <div className="storybook-color-blocks">
    {colors.map(([name, token, value]) => (
      <article className="storybook-color-block" key={token}>
        <div
          aria-label={`${name} ${value}`}
          className="storybook-color-block__swatch"
          style={{ backgroundColor: value }}
        />
        <div className="storybook-color-block__details">
          <strong>{name}</strong>
          <span>{token} / {value}</span>
        </div>
      </article>
    ))}
  </div>
);

<Meta title="Foundations/Color" />

# 色

画面とStorybookで共有する色トークンです。配色ごとの色面と値を並べています。

## 明るい配色

<ColorBlocks colors={lightColors} />

## 暗い配色

<ColorBlocks colors={darkColors} />
